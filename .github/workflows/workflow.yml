name: Deploy React APP

on: 
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build react app
    steps:
      - uses: actions/checkout@v3
      - name: Go to gitworkspace
      - run: echo "hello"
       